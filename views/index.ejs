<!DOCTYPE html>
<html>
<head>
  <title>Code Redemption</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

</head>
<body>
    <nav class="navbar bg-warning">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">
            <img src="/images/Redeem.png" alt="Logo" width="200" height="24" class="d-inline-block align-text-top">
           
          </a>
        </div>
      </nav>
<div class="container" style="margin-top: 20px;">
    <div class="mt-10">
        <div class="row">
            <div class="col-md-6">    
                        <div class="form-group">
                          <label>Order ID:</label>
                          <input id="orderID" type="text" class="form-control"   placeholder="orderID">
                        </div>
                        <div class="form-group">
                          <label>Purchased Product:</label>
                          <input id="purchasedProduct" type="text" class="form-control"   placeholder="Purchased Product">
                        </div>
                        <div class="form-group">
                          <label>Email:</label>
                          <input id="email" type="text" class="form-control"   placeholder="Email:">
                        </div>
                        <div class="form-group">
                          <label>Your Redeem Code:</label>
                          <input id="redeemCode" type="text" class="form-control"   placeholder="Your Redeem Code:">
                        </div>
                        <button id="redeemButton"  class="btn bg-warning text-white" style="margin-top: 12px;">Redeem</button>
                 
    <!-- old design  -->
                    <!-- <div class="form-box">
                      <h1 class="redeem-title">REDEEM YOUR CODE:</h1>
                      <div class="field">
                        <label class="label field-label">Order ID:</label>
                        <div class="control">
                          <input id="orderID" class="input" type="text" placeholder="Enter your order ID" required>
                        </div>
                      </div>
                      <div class="field">
                        <label class="label field-label">Purchased Product:</label>
                        <div class="control">
                          <input id="purchasedProduct" class="input" type="text" placeholder="Enter the purchased product" required>
                        </div>
                      </div>
                      <div class="field">
                        <label class="label field-label">Email:</label>
                        <div class="control">
                          <input id="email" class="input" type="email" placeholder="Enter your email" required>
                        </div>
                      </div>
                      <div class="field">
                        <label class="label field-label">Your Redeem Code:</label>
                        <div class="control">
                          <input id="redeemCode" class="input" type="text" placeholder="Enter your redeem code" required>
                        </div>
                      </div>
                      <div class="field">
                        <div class="control">
                          <button id="redeemButton" class="button redeem-button">Redeem</button>
                        </div>
                      </div>
                    </div> -->
            </div>
            <div class="col-md-6"> 
              <h2>Instructions:</h2>
              <p>
              Dear customer, thank you for visiting our website, to receive the product in the right way please redeem the received code on this website and enter the correct data of the order ID and Email, after that we will deliver the purchased product to you via email              </p></div>
        </div>
</div>
  
   
</div>   

  
  
  <!-- <div class="redeem-container">
    <div class="logo">
      <img src="/images/Redeem.png" alt="Logo">
    </div>
    <div class="form-box">
      <h1 class="redeem-title">REDEEM YOUR CODE:</h1>
      
        <div class="field">
          <label class="label field-label">Order ID:</label>
          <div class="control">
            <input  id="orderID" class="input" type="text" placeholder="Enter your order ID" required>
          </div>
        </div>
        <div class="field">
          <label class="label field-label">Email:</label>
          <div class="control">
            <input id="email" class="input" type="email" placeholder="Enter your email" required>
          </div>
        </div>
        <div class="field">
          <label class="label field-label">Your Redeem Code:</label>
          <div class="control">
            <input id="redeemCode" class="input" type="text" placeholder="Enter your redeem code" required>
          </div>
        </div>
        <div class="field">
          <div class="control">
            <button id="redeemButton" class="button redeem-button">Redeem</button>
          </div>
        </div>
      
    </div>
  </div> -->

  <script src="https://unpkg.com/sweetalert/dist/sweetalert.min.js"></script>
    <!-- <script>
    // Get form elements
const orderIDInput = document.querySelector('#orderID');
const emailInput = document.querySelector('#email');
const redeemCodeInput = document.querySelector('#redeemCode');
const redeemButton = document.querySelector('#redeemButton');

// Add event listener to the redeem button
redeemButton.addEventListener('click', () => {
  // Get the values from the input fields
  const orderID = orderIDInput.value;
  const email = emailInput.value;
  const redeemedCode = redeemCodeInput.value;

  // Create a data object with the form values
  const data = {
    orderID,
    email,
    redeemedCode
  };

  // Send the data to the server
  fetch('/redeem-code', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(data)
  })
    .then(response => response.json())
    .then(result => {
      // Handle the response from the server
      console.log(result);
      // Display appropriate message to the user
      if (result.success) {
      swal("Success!", "Code redeemed successfully!", "success");

        // Code redemption successful
        // alert('Code redeemed successfully!');
      } else {
        swal("Error!", "Failed to redeem code!", "error");
        // Code redemption failed
        // alert('Failed to redeem code. Please try again.');
      }
    })
    .catch(error => {
      console.error('Error:', error);
      // Display error message to the user
      swal("Error!",  'Failed to redeem code!', "error");
    });
}); -->
<script>

  // Get form elements
const orderIDInput = document.querySelector('#orderID');
const purchasedProductInput = document.querySelector('#purchasedProduct');
const emailInput = document.querySelector('#email');
const redeemCodeInput = document.querySelector('#redeemCode');
const redeemButton = document.querySelector('#redeemButton');

// Add event listener to the redeem button
redeemButton.addEventListener('click', () => {
  // Get the values from the input fields
  const orderID = orderIDInput.value;
  const purchasedProduct = purchasedProductInput.value;
  const email = emailInput.value;
  const redeemedCode = redeemCodeInput.value;

if(orderID==''|| purchasedProduct==''|| email ==''|| redeemedCode==''||orderID==' '|| purchasedProduct==' '|| email ==' '|| redeemedCode==' '){
swal("Error!", 'All input fields are required', "error");
return
}

  // Create a data object with the form values
  const data = {
    orderID,
    purchasedProduct,
    email,
    redeemedCode
  };

  // Send the data to the server
  fetch('/redeem-code', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(data)
  })
    .then(response => response.json())
    .then(result => {
      // Handle the response from the server
      console.log(result);
      // Display appropriate message to the user
      if (result.success) {
        swal("Success!", "Code redeemed successfully!", "success");
        // Code redemption successful
        // alert('Code redeemed successfully!');
      } else {
        swal("Error!", "Failed to redeem code!", "error");
        // Code redemption failed
        // alert('Failed to redeem code. Please try again.');
      }
    })
    .catch(error => {
      console.error('Error:', error);
      // Display error message to the user
      swal("Error!", 'Failed to redeem code!', "error");
    });
});


</script>
</body>
</html>
